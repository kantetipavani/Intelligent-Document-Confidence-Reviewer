- Goal: INFO tab should show extracted invoice fields as "JSON-style" output that matches the table columns (INVOICE NO, VENDOR, AMOUNT, DATE, GSTIN, STATUS).
- Left panel behavior: clicking a "module" (selected JSON row/field) should update the right panel table so the extracted fields table gets populated.
- Implementation plan will be decided after inspecting activity payload and the shape expected by <ExtractedFields />.

