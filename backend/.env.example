# Copy to backend/.env and fill values

# SMTP for sending OTP / reset password emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=pavanikanteti55@gmail.com
SMTP_PASSWORD=wvqo ueqs xxrk gihe
SMTP_FROM=pavanikanteti55@gmail.com
# Set to true to fail fast when SMTP creds are missing
SMTP_STRICT=true

