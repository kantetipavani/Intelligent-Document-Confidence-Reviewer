# T2 Intelligent Document Confidence Reviewer

(Scaffold in progress)

## What this includes
- FastAPI backend (multi-tenant, versioned extraction + human review)
- Next.js frontend (split-pane PDF/text + extracted fields + inline review)
- LLM extraction layer (structured JSON output with per-field confidence)

## Run (after scaffold)
- Backend: `cd backend && ...`
- Frontend: `cd frontend && ...`

